{
  "name": "pentum",
  "version": "0.1.0",
  "private": true,
  "description": "PokeRogue battle bot that asks TypeSafe Jev which move to use",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bot": "node src/bot.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0",
    "playwright": "^1.55.0"
  }
}
